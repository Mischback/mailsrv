# ``tox`` is used as a utility to manage several development-related virtual
# environments.
#
# These environments are not directly related to the actual purpose of the
# project, so a real pinning is not required. The specified value is just the
# minimal requirement.
tox>=3.26
